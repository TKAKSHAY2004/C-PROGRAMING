#include <stdio.h>
void main() {
int i, intsum;
printf("i = ");
scanf("%d", &i);// read value of i
char ch = 'a'; // assing character 'a' to ch
float floatsum;
intsum = ch + i; // add values of it and ch

floatsum = ch + i; // add values i and chi 
printf("Integer result %d, Float result = %f\n", intsum, floatsum );

}
